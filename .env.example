# Copy to .env.local (or .env) for local development. Create React App
# only exposes variables prefixed with REACT_APP_ to the browser bundle -
# never put a secret here, it ends up in client-side JS.

REACT_APP_BASE_URL=http://localhost:8800
