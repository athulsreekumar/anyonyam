// jest-dom adds custom jest matchers for asserting on DOM nodes, e.g.
// expect(element).toHaveTextContent(/react/i). Auto-loaded by CRA's test
// runner because this file is named setupTests.js.
import '@testing-library/jest-dom';
